export interface NavLink {
	title: string
	path: string
}

export interface ContactLink {
	title: string
	url: string
	icon: JSX.Element
}
export interface ContactFormInputs {
	name: string
	email: string
	phone: string
	message: string
}

export interface NavLink {
	title: string
	path: string
}

export interface ContactLink {
	title: string
	url: string
	icon: JSX.Element
}

export interface Skill {
	title: string
	tools: string[]
	icon: JSX.Element
}
